@model IEnumerable<SistemaIMC.Models.T_CategoriaIMC>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCategoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RangoMinIMC)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RangoMaxIMC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCategoria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RangoMinIMC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RangoMaxIMC)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_CategoriaIMC">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_CategoriaIMC">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_CategoriaIMC">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
