@* Ubicación: SistemaIMC/Views/T_CategoriaIMC/Index.cshtml *@

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model List<SistemaIMC.Models.T_CategoriaIMC>
@{
    ViewData["Title"] = "Gestión de Categorías IMC";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0 text-gray-800"><i class="fas fa-chart-pie me-2"></i>Categorías de IMC</h1>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus me-1"></i> Nueva Categoría
        </a>
    </div>

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Lista de Categorías de IMC</h6>
        </div>
        <div class="card-body">
            @if (Model != null && Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-bordered table-hover" width="100%" cellspacing="0">
                        <thead class="table-light">
                            <tr>
                                <th>ID</th>
                                <th>Nombre de la Categoría</th>
                                <th>Rango Mínimo</th>
                                <th>Rango Máximo</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var categoria in Model)
                            {
                                <tr>
                                    <td>@categoria.ID_CategoriaIMC</td>
                                    <td>@categoria.NombreCategoria</td>
                                    <td>@categoria.RangoMinIMC</td>
                                    <td>@categoria.RangoMaxIMC</td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-action="Edit" asp-route-id="@categoria.ID_CategoriaIMC"
                                               class="btn btn-warning btn-sm">
                                                <i class="fas fa-edit"></i> Editar
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@categoria.ID_CategoriaIMC"
                                               class="btn btn-danger btn-sm">
                                                <i class="fas fa-trash"></i> Eliminar
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-4">
                    <i class="fas fa-chart-pie fa-3x text-gray-300 mb-3"></i>
                    <h5 class="text-gray-500">No hay categorías registradas</h5>
                    <p class="text-gray-500">Comienza agregando una nueva categoría.</p>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="fas fa-plus me-1"></i> Crear Primera Categoría
                    </a>
                </div>
            }
        </div>
    </div>
</div>