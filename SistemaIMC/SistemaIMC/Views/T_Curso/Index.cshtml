@model IEnumerable<SistemaIMC.Models.T_Curso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Establecimiento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Establecimiento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Curso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Curso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Curso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
