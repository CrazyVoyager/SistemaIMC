@model IEnumerable<SistemaIMC.Models.T_Comuna>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreComuna)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Region)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreComuna)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Region)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Comuna">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Comuna">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Comuna">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
