@model IEnumerable<SistemaIMC.Models.T_Establecimiento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstablecimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Comuna)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstablecimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Comuna)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Establecimiento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Establecimiento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Establecimiento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
